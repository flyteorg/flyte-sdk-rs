# Build context filter for RUST_IMAGE_MODE=build (see rust_task.py).
# NOTE: providing a .dockerignore REPLACES the SDK's standard ignore patterns,
# so everything to exclude must be listed here.
target
**/target
.git
**/.git
__pycache__
**/__pycache__
*.pyc
.DS_Store
